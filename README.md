# Bookstore app

## Run (from this folder)


Run `coe528project.gui.BookStoreApp`

`books.txt` and `customers.txt` are read from the current directory.

## Sign in (test data)

| Role | Username | Password |
|------|----------|----------|
| Owner (admin) | `admin` | `admin` |
| Customer | `demo` | `demo` |

Sample books are listed in `books.txt` (name,price per line).
